#include "main.h"

/**
 * add - check for alphabet
 *
 *@a:  this is the argument
 *
 *@b:  this is the argument
 *
 *Return: bringing result
 *
 */
int add(int a, int b);
int add(int a, int b)
{
int result = (a + b);
return (result);
}
